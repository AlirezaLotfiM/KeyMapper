<Window x:Class="KeyMapper.QuickAccessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KeyMapper Quick Access Menu" 
        Height="400" 
        Width="320"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True" 
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        FontFamily="Segoe UI"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <!-- Search TextBox style -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#26384F"/>
            <Setter Property="BorderBrush" Value="#C8B991"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="CaretBrush" Value="#318D99"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" VerticalAlignment="Center"/>
                                <!-- Placeholder -->
                                <TextBlock Text="Search expansions &amp; actions..." Foreground="#6B7B8F" FontSize="13" IsHitTestVisible="False" Margin="10,0" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#318D99"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ListBox Style -->
        <Style x:Key="PaletteListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="#E1D3AE" BorderThickness="0,0,0,1" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#CDEFF0"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#FFF0C9"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#FFF8E7" BorderBrush="#40536C" BorderThickness="3">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="5" Color="#6B5A3A" Opacity="0.35"/>
        </Border.Effect>
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search Area -->
            <TextBox x:Name="SearchBox" Grid.Row="0" Style="{StaticResource SearchTextBoxStyle}" Height="30" Margin="0,0,0,10" TextChanged="SearchBox_TextChanged"/>

            <!-- Selection Preview Header -->
            <Border x:Name="SelectionHeader" Grid.Row="1" Background="#FFF0C9" BorderBrush="#D6C59C" BorderThickness="1" Padding="8,6" Margin="0,0,0,10" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Selected text preview" Foreground="#6B7B8F" FontSize="10" FontWeight="SemiBold"/>
                    <TextBlock x:Name="SelectionTextPreview" Foreground="#26384F" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Menu Items List -->
            <ListBox x:Name="MenuListBox" Grid.Row="2" Style="{StaticResource PaletteListBoxStyle}" MouseDoubleClick="MenuListBox_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon badge container -->
                            <Border Grid.Column="0" Width="26" Height="26" CornerRadius="13" Background="{Binding IconBgColor}" Margin="0,0,8,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe UI Emoji" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{Binding IconFgColor}"/>
                            </Border>
                            
                            <StackPanel Grid.Column="1" Margin="6,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" Foreground="#26384F" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding SubTitle}" Foreground="#6B7B8F" FontSize="11" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <Border Grid.Column="2" Background="#CDEFF0" BorderBrush="#8FC6CB" BorderThickness="1" Padding="4,2" VerticalAlignment="Center" Margin="5,0,0,0">
                                <TextBlock Text="{Binding TypeDisplay}" Foreground="#217783" FontSize="9" FontWeight="SemiBold"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
